import React from 'react'

export const First = ({ onSelect }) => {
    return (
      <div>
        <button style={{ backgroundColor: "#FFC202" ,fontSize:"25px"}} onClick={onSelect}>KOLAY</button>
      </div>
    );
  };