import React from 'react'

const Third = ({ onSelect }) => {
    return (
      <div>
        <button style={{ backgroundColor: "red",fontSize:"25px" }} onClick={onSelect}>ZOR</button>
      </div>
    );
  };
  
export default Third